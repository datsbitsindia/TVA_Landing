version: '3.8'

services:
  tva-landing:
    build: .
    container_name: tva-landing-web
    network_mode: host
    environment:
      - PORT=8090
      - DB_HOST=127.0.0.1
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=BHAVIN467
      - DB_NAME=tva_db
      - TABLE_PREFIX=tva_
      - NODE_ENV=production
    restart: always
